import {
  useIntl
} from "./chunk-6VCP4QJE.js";
import "./chunk-L3V7B3MO.js";
import {
  Box,
  Field,
  Flex
} from "./chunk-RC54WROU.js";
import "./chunk-V6VTA2ZA.js";
import "./chunk-3DNG6OI4.js";
import "./chunk-AWHRF5BE.js";
import {
  require_jsx_runtime
} from "./chunk-LMPM4PM5.js";
import "./chunk-3HIH53M2.js";
import {
  require_react
} from "./chunk-D3I3COXH.js";
import {
  __toESM
} from "./chunk-LK32TJAX.js";

// node_modules/@webbycrown/advanced-fields/dist/_chunks/index-DXVpJkpj.mjs
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
var import_react = __toESM(require_react(), 1);
var AdvancedCheckbox = ({
  attribute = {},
  description = { id: "", defaultMessage: "" },
  disabled,
  error,
  intlLabel = { id: "", defaultMessage: "" },
  labelAction,
  name,
  onChange,
  required,
  value
}) => {
  var _a;
  const { formatMessage } = useIntl();
  const {
    checkboxType = "multiple",
    layout = "vertical",
    minChoices = 0,
    maxChoices = 0,
    defaultSelected = "",
    checkboxOptions = "",
    customErrorMessage = "",
    fieldNote = ""
  } = attribute.options || attribute;
  const fieldNoteFromAttribute = ((_a = attribute.options) == null ? void 0 : _a.fieldNote) || "";
  const getInitialValues = () => {
    if (checkboxType === "single") {
      if (value && Array.isArray(value) && value.length > 0) {
        return value;
      } else if (value && typeof value === "string" && value.trim()) {
        return [value.trim()];
      } else if (defaultSelected && typeof defaultSelected === "string" && defaultSelected.trim()) {
        return [defaultSelected.trim()];
      } else if (defaultSelected && Array.isArray(defaultSelected) && defaultSelected.length > 0) {
        return defaultSelected;
      }
      return [];
    } else {
      if (value && Array.isArray(value)) {
        return value;
      } else if (value && typeof value === "string") {
        return value.split(",").map((v) => v.trim()).filter((v) => v);
      } else if (defaultSelected) {
        return defaultSelected.split(",").map((v) => v.trim()).filter((v) => v);
      }
      return [];
    }
  };
  const [fieldValue, setFieldValue] = (0, import_react.useState)(getInitialValues);
  const [validationError, setValidationError] = (0, import_react.useState)(null);
  const [hasInteracted, setHasInteracted] = (0, import_react.useState)(false);
  const [isInitialized, setIsInitialized] = (0, import_react.useState)(false);
  const options = checkboxOptions.split("\n").filter((opt) => opt.trim()).map((opt) => {
    const [value2, label] = opt.split("|");
    return { value: (value2 == null ? void 0 : value2.trim()) || "", label: (label == null ? void 0 : label.trim()) || (value2 == null ? void 0 : value2.trim()) || "" };
  });
  (0, import_react.useEffect)(() => {
    const initialValues = getInitialValues();
    setFieldValue(initialValues);
    const validationResult = validateSelection(initialValues);
    setValidationError(validationResult);
    if (onChange && initialValues.length > 0 && (!value || Array.isArray(value) && value.length === 0) && !isInitialized) {
      setTimeout(() => {
        onChange({
          target: {
            value: initialValues,
            name,
            id: name
          }
        });
        setIsInitialized(true);
      }, 0);
    } else if (!isInitialized) {
      setIsInitialized(true);
    }
  }, []);
  (0, import_react.useEffect)(() => {
    if (value && Array.isArray(value) && value.length > 0) {
      setFieldValue(value);
      const validationResult = validateSelection(value);
      setValidationError(validationResult);
    }
  }, [value]);
  const validateSelection = (val) => {
    const values = Array.isArray(val) ? val : [];
    if (required && values.length === 0) {
      return customErrorMessage || "This field is required";
    }
    if (values.length === 0) {
      return null;
    }
    if (checkboxType === "multiple") {
      if (minChoices > 0 && values.length < minChoices) {
        return customErrorMessage || `Please select at least ${minChoices} option${minChoices > 1 ? "s" : ""}`;
      }
      if (maxChoices > 0 && values.length > maxChoices) {
        return customErrorMessage || `Please select at most ${maxChoices} option${maxChoices > 1 ? "s" : ""}`;
      }
    }
    return null;
  };
  const handleCheckboxChange = (optionValue, isChecked) => {
    let newValue;
    if (checkboxType === "single") {
      if (isChecked) {
        newValue = [optionValue];
      } else {
        newValue = [];
      }
    } else {
      if (isChecked) {
        newValue = [...fieldValue, optionValue];
      } else {
        newValue = fieldValue.filter((val) => val !== optionValue);
      }
    }
    setFieldValue(newValue);
    setHasInteracted(true);
    const error2 = validateSelection(newValue);
    setValidationError(error2);
    if (onChange) {
      onChange({
        target: {
          value: newValue,
          name,
          id: name
        }
      });
    }
  };
  const displayError = error || hasInteracted && validationError;
  const renderCheckboxes = () => {
    const checkboxStyle = {
      display: "flex",
      alignItems: "center",
      gap: "8px",
      marginBottom: "8px"
    };
    const checkboxInputStyle = {
      width: "16px",
      height: "16px",
      accentColor: "#4945ff",
      margin: "0",
      padding: "0",
      opacity: "1",
      visibility: "visible",
      display: "block",
      position: "relative",
      zIndex: "1"
    };
    const checkboxLabelStyle = {
      fontSize: "14px",
      fontFamily: "inherit",
      cursor: "pointer",
      userSelect: "none"
    };
    if (checkboxType === "single") {
      if (!options || options.length === 0) {
        return (0, import_jsx_runtime.jsx)("div", { style: { padding: "8px", color: "#666", fontStyle: "italic" }, children: formatMessage({
          id: "advanced-fields.checkbox.options.checkboxOptions.description",
          defaultMessage: "Define available options for multiple checkboxes (one per line: value|label)"
        }) });
      }
      if (layout === "horizontal") {
        return (0, import_jsx_runtime.jsx)(Flex, { wrap: "wrap", gap: 2, children: options.map((option) => (0, import_jsx_runtime.jsxs)("div", { style: checkboxStyle, children: [
          (0, import_jsx_runtime.jsx)(
            "input",
            {
              type: "checkbox",
              id: `${name}-${option.value}`,
              checked: fieldValue.includes(option.value),
              onChange: (e) => handleCheckboxChange(option.value, e.target.checked),
              disabled,
              style: checkboxInputStyle
            }
          ),
          (0, import_jsx_runtime.jsx)(
            "label",
            {
              htmlFor: `${name}-${option.value}`,
              style: checkboxLabelStyle,
              children: option.label
            }
          )
        ] }, option.value)) });
      }
      return (0, import_jsx_runtime.jsx)("div", { children: options.map((option) => (0, import_jsx_runtime.jsxs)("div", { style: checkboxStyle, children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            id: `${name}-${option.value}`,
            checked: fieldValue.includes(option.value),
            onChange: (e) => handleCheckboxChange(option.value, e.target.checked),
            disabled,
            style: checkboxInputStyle
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "label",
          {
            htmlFor: `${name}-${option.value}`,
            style: checkboxLabelStyle,
            children: option.label
          }
        )
      ] }, option.value)) });
    }
    if (!options || options.length === 0) {
      return (0, import_jsx_runtime.jsx)("div", { style: { padding: "8px", color: "#666", fontStyle: "italic" }, children: formatMessage({
        id: "advanced-fields.checkbox.options.checkboxOptions.description",
        defaultMessage: "Define available options for multiple checkboxes (one per line: value|label)"
      }) });
    }
    if (layout === "horizontal") {
      return (0, import_jsx_runtime.jsx)(Flex, { wrap: "wrap", gap: 2, children: options.map((option) => (0, import_jsx_runtime.jsxs)("div", { style: checkboxStyle, children: [
        (0, import_jsx_runtime.jsx)(
          "input",
          {
            type: "checkbox",
            id: `${name}-${option.value}`,
            checked: fieldValue.includes(option.value),
            onChange: (e) => handleCheckboxChange(option.value, e.target.checked),
            disabled,
            style: checkboxInputStyle
          }
        ),
        (0, import_jsx_runtime.jsx)(
          "label",
          {
            htmlFor: `${name}-${option.value}`,
            style: checkboxLabelStyle,
            children: option.label
          }
        )
      ] }, option.value)) });
    }
    return (0, import_jsx_runtime.jsx)("div", { children: options.map((option) => (0, import_jsx_runtime.jsxs)("div", { style: checkboxStyle, children: [
      (0, import_jsx_runtime.jsx)(
        "input",
        {
          type: "checkbox",
          id: `${name}-${option.value}`,
          checked: fieldValue.includes(option.value),
          onChange: (e) => handleCheckboxChange(option.value, e.target.checked),
          disabled,
          style: checkboxInputStyle
        }
      ),
      (0, import_jsx_runtime.jsx)(
        "label",
        {
          htmlFor: `${name}-${option.value}`,
          style: checkboxLabelStyle,
          children: option.label
        }
      )
    ] }, option.value)) });
  };
  return (0, import_jsx_runtime.jsx)(Box, { col: 6, children: (0, import_jsx_runtime.jsxs)(Field.Root, { name, error: displayError, children: [
    (0, import_jsx_runtime.jsxs)(Field.Label, { children: [
      intlLabel.id ? formatMessage(intlLabel) : intlLabel.defaultMessage || name,
      required && (0, import_jsx_runtime.jsx)("span", { style: { color: "#d02b20", marginLeft: "4px" }, children: "*" })
    ] }),
    renderCheckboxes(),
    displayError && (0, import_jsx_runtime.jsx)(Field.Error, { children: displayError }),
    description && (description.id || description.defaultMessage) && (0, import_jsx_runtime.jsx)(Field.Hint, { children: description.id ? formatMessage(description) : description.defaultMessage }),
    (fieldNote || fieldNoteFromAttribute) && (0, import_jsx_runtime.jsx)("span", { style: {
      fontStyle: "italic",
      color: "#666",
      fontSize: "12px",
      display: "block",
      marginTop: "4px"
    }, children: fieldNote || fieldNoteFromAttribute })
  ] }) });
};
export {
  AdvancedCheckbox as default
};
//# sourceMappingURL=index-DXVpJkpj-YRSNMTKP.js.map
